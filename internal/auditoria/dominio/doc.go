// Package dominio contiene la bitácora forense append-only y el
// hash-chaining del bounded context Auditoría (ADR 0005). Pendiente de
// diseño e implementación por el agente auditoria-forense.
package dominio
